import 'package:flutter/material.dart';

class MyAddsPages extends StatelessWidget {
  const MyAddsPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
